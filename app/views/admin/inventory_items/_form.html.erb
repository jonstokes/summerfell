<%= form_for(@admin_inventory_item) do |f| %>
  <% if @admin_inventory_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_inventory_item.errors.count, "error") %> prohibited this admin_inventory_item from being saved:</h2>

      <ul>
      <% @admin_inventory_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
