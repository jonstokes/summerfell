<%= form_for(@admin_hotspot_registration) do |f| %>
  <% if @admin_hotspot_registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_hotspot_registration.errors.count, "error") %> prohibited this admin_hotspot_registration from being saved:</h2>

      <ul>
      <% @admin_hotspot_registration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
